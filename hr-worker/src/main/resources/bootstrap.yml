spring:
  profiles:
    active: prod
  application:
    name: hr-worker  cloud:
    config:
      uri: http://hr-config-server:8888

management:
  endpoints:
    web:
      exposure:
        include: '*'